import { Vector3 } from '/three/build/three.module.js';

export default class PlayerLogic {
    constructor() {

    }

    test() {
        let v = new Vector3(1, 2, 3)
        return "player test ${v}"
    }
}